{
  "name": "quilnode-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node scripts/build-brand.mjs && node scripts/strip-image-metadata.mjs && vite",
    "build": "node scripts/build-brand.mjs && node scripts/strip-image-metadata.mjs && vite build && node scripts/prepare-sites-build.mjs",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run format:check && npm run build && npm test",
    "format": "prettier --write src scripts tests worker guide index.html vite.config.mjs vercel.json package.json README.md",
    "format:check": "prettier --check src scripts tests worker guide index.html vite.config.mjs vercel.json package.json README.md",
    "test:sites": "node --test tests/sites-worker.test.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "5.3.0",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "react-icons": "5.7.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "ignore": "7.0.8",
    "prettier": "3.9.6",
    "vite": "6.4.3"
  }
}
